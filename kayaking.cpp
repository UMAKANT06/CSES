#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,k;
    cin>>n;
    vector<int>a;
    for(int i=0;i<n;i++){
        cin>>k;
        a.push_back(k);
    }

    sort(a.begin(),a.end());
    
}